apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultStaticSecret
metadata:
  name: dex-secrets
  namespace: dex
spec:
  vaultAuthRef: default
  mount: secret
  type: kv-v2
  path: dex/dex-secrets
  refreshAfter: "1h"
  destination:
    name: dex-secrets
    create: true
    overwrite: true
