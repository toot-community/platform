apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: hourly-volume-backup
  namespace: longhorn-system
spec:
  concurrency: 3
  cron: 0 * * * *
  groups:
  - default
  name: hourly-volume-backup
  retain: 24
  task: backup
