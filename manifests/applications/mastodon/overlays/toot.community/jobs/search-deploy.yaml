apiVersion: batch/v1
kind: Job
metadata:
  labels:
    job-name: mastodon-search-deploy
  name: mastodon-search-deploy
  namespace: toot-community
spec:
  parallelism: 1
  podReplacementPolicy: TerminatingOrFailed
  selector:
    matchLabels:
      job-name: mastodon-search-deploy
  suspend: false
  template:
    metadata:
      labels:
        job-name: mastodon-search-deploy
      name: mastodon-search-deploy
    spec:
      restartPolicy: Never
      containers:
      - command:
        - bash
        - -c
        - bin/tootctl search deploy --concurrency 16 --batch_size 4096
        env:
        - name: ES_PASS
          valueFrom:
            secretKeyRef:
              key: elastic
              name: elasticsearch-es-elastic-user
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              key: username
              name: database-app
        - name: DB_PASS
          valueFrom:
            secretKeyRef:
              key: password
              name: database-app
        envFrom:
        - configMapRef:
            name: mastodon-env
        - secretRef:
            name: mastodon-secrets-env
        image: ghcr.io/mastodon/mastodon:v4.4.5@sha256:e8ad3b126c71ed5d7df24d1257754a45338d5158e9dd88255accdf958a676ca6
        imagePullPolicy: IfNotPresent
        name: search-deploy
        resources:
          limits:
            memory: 8Gi
          requests:
            cpu: "4"
            memory: 8Gi