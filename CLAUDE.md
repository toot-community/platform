# Project Rules

## CLI Tools

- Always run `vault` commands through `direnv exec` (e.g., `direnv exec /Users/jorijn/Development/tc vault ...`) to ensure the correct environment variables (VAULT_ADDR, VAULT_TOKEN, etc.) are loaded.
