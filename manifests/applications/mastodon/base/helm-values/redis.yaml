architecture: replication

auth:
  enabled: false
  sentinel: false

replica:
  replicaCount: 3

sentinel:
  enabled: true
  resources:
    requests:
      cpu: 100m
      memory: 512Mi
    limits:
      memory: 512Mi

metrics: 
  enabled: &metricsEnabled true
  serviceMonitor: 
    enabled: *metricsEnabled