apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: microblog-network-https
spec:
  commonName: microblog.network
  dnsNames:
    - microblog.network
    - "*.microblog.network"
  issuerRef:
    name: letsencrypt-route53
    kind: ClusterIssuer
  secretName: microblog-network-tls
