apiVersion: secrets.hashicorp.com/v1beta1
kind: VaultAuth
metadata:
  name: default
  namespace: argocd
spec:
  vaultConnectionRef: vault/default
  method: kubernetes
  mount: kubernetes
  kubernetes:
    role: argocd
    serviceAccount: default
  vaultAuthGlobalRef:
    name: default
    namespace: vault
    allowDefault: true
