apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: default-deny-ingress
  namespace: vault
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
# Allow Raft replication between Vault pods (port 8201)
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vault-raft
  namespace: vault
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: vault
      component: server
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: vault
              component: server
      ports:
        - port: 8201
          protocol: TCP
  policyTypes:
    - Ingress
---
# Allow API/UI access on port 8200 from within the cluster
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vault-api
  namespace: vault
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: vault
      component: server
  ingress:
    - from:
        - namespaceSelector: {}
      ports:
        - port: 8200
          protocol: TCP
  policyTypes:
    - Ingress
---
# Allow VictoriaMetrics scraping of VSO metrics
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: vso-metrics
  namespace: vault
spec:
  podSelector:
    matchLabels:
      control-plane: controller-manager
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: victoriametrics
      ports:
        - port: 8443
          protocol: TCP
  policyTypes:
    - Ingress
