cluster:
  network:
    podSubnets:
      - 10.0.16.0/20
    serviceSubnets:
      - 10.0.8.0/21  
machine:
  kubelet:
    nodeIP:
      validSubnets:
        - ${vpc_subnet_cidr}
        - ${vswitch_subnet_cidr}